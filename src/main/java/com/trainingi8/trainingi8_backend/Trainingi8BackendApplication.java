package com.trainingi8.trainingi8_backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Annotation to mark this class as a Spring Boot application
@SpringBootApplication
public class Trainingi8BackendApplication {

	// Main method to run the Spring Boot application
	public static void main(String[] args) {
		SpringApplication.run(Trainingi8BackendApplication.class, args);
	}

}
